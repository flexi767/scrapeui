import { MarketplacePickerClient } from "@/components/facebook-marketplace/MarketplacePickerClient";

export const metadata = {
  title: "scrapeui Marketplace Picker",
};

export default function FacebookMarketplacePickerPage() {
  return <MarketplacePickerClient />;
}
